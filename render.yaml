services:
  - type: web
    name: ipl-auction-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python Dashboard/build_dashboard_data.py
    startCommand: python Dashboard/server.py
    autoDeploy: true
    envVars:
      - key: LEAGUE_MC_ITERATIONS
        value: "50"
