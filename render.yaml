services:
  - type: web
    name: ipl-auction-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && LEAGUE_MC_ITERATIONS=100 python Dashboard/build_dashboard_data.py
    startCommand: python Dashboard/server.py
    autoDeploy: true
